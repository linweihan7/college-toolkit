# Local (offline) transcription + on-device speaker diarization.
# Heavier install (pulls in PyTorch). Only needed if you use the "local" engine
# or want speaker labels.
faster-whisper>=1.0.0
torch>=2.1
torchaudio>=2.1

# Speaker labels, no-token backend (voice embeddings + clustering):
resemblyzer>=0.1.3
scikit-learn>=1.3

# Speaker labels, higher-accuracy backend (needs HUGGINGFACE_TOKEN):
pyannote.audio>=3.1
